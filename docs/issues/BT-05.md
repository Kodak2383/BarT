# BT-05 — Starting-point proposal

**Status:** ❌ **Dropped 2026-09-17** — superseded by the decision in PRD §7.

## Why it is gone
BarT can no longer move items at all (BT-15), so it cannot propose an arrangement either. The welcome window explains the ⌘-drag instead — BT-16.

Kept as a record so the plan's history stays readable.
