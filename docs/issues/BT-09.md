# BT-09 — Drag icons between sections

**Status:** ❌ **Dropped 2026-09-17** — superseded by the decision in PRD §7.

## Why it is gone
Dropping an icon somewhere would have to move the real item, which is exactly the capability BT-15 removes. The user drags in the menu bar itself now — BT-16.

Kept as a record so the plan's history stays readable.
