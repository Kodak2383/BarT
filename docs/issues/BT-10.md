# BT-10 — Rework "could not be positioned"

**Mode:** 🤖 AFK · **needs:** BT-07 · **blocks:** BT-14

## Why
Today: a grey box with a paragraph of prose, no dismiss, and advice ("selecting it in the list
again") that stops matching the UI once BT-07 lands (PRD §3.5).

## Slice
Controller error state → compact UI with a real action.

## Visible result
One line naming the item, a disclosure for the explanation, **Retry**, and a way to dismiss it.

## Done when
- Summary line names the item; the paragraph lives behind a disclosure.
- "Retry" calls `moveItem` for that key again (which also releases the oscillation brake).
- The message can be dismissed and does not come back until the next failure.
- Wording no longer refers to selecting things in a list.

## Files
`BarT/UI/SettingsView.swift`, `BarT/Engine/MenuBarController.swift`

## How to trigger it
`OscillationGuard` gives up after three attempts on the same key — two adjacent third-party items
(the Stats pair is the known case) reproduce it.
