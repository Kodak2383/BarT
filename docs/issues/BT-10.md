# BT-10 — Rework 'could not be positioned'

**Status:** ❌ **Dropped 2026-09-17** — superseded by the decision in PRD §7.

## Why it is gone
The message reports a failure of the drag engine. With the engine gone (BT-15), so is the failure — along with OscillationGuard, which existed only for it.

Kept as a record so the plan's history stays readable.
